import 'package:flutter/material.dart';

class RopeSkippingAvatar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.fitness_center,
      size: 60,
      color: Colors.white,
    );
  }
}
