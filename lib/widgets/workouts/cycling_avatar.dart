import 'package:flutter/material.dart';

class CyclingAvatar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.directions_bike,
      size: 60,
      color: Colors.white,
    );
  }
}
