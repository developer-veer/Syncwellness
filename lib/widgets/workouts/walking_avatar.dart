import 'package:flutter/material.dart';

class WalkingAvatar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.directions_walk,
      size: 60,
      color: Colors.white,
    );
  }
}
